apiVersion: v1
kind: Secret
metadata:
  name: object-detection-secrets
  namespace: object-detection
type: Opaque
data:
  # Base64 encoded values - replace with actual values
  # echo -n "redis://your-redis-url:6379" | base64
  REDIS_URL: cmVkaXM6Ly95b3VyLXJlZGlzLXVybDo2Mzc5
  # AWS credentials will be managed via IAM roles for service accounts (IRSA)
  # Include these only if not using IRSA
  # AWS_ACCESS_KEY_ID: ""
  # AWS_SECRET_ACCESS_KEY: ""